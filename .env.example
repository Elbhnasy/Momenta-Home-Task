# Copy to .env and fill these in.
GROQ_API_KEY=
ELEVENLABS_API_KEY=
# Embeddings run locally (fastembed) — no key needed.
# ElevenLabs serves both STT (Scribe) and TTS; Deepgram was benchmarked and dropped
# (see data/docs/PROMPT_LOG.md) — only 2 credentials needed, not 3.
